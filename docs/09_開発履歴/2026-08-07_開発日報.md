# WithTama 開発日報

日付：2026-08-07

## 1. 本日の成果

- 管理者機能の現状調査（admin 画面・role・RLS・認証）
- 管理者審査機能の設計確定（Decision No.96〜105）
- 管理者認証基盤の実装（`app_metadata.role = admin`、route guard）
- `pet_review_logs` Migration 作成
- **pets RLS 本番化**（`20260807120000_harden_pets_rls.sql`）
- **pets status 遷移 Trigger** 設計確定・Migration 作成（`20260807130000_enforce_pets_status_transition.sql`）
- pets status / RLS **セキュリティ試験**（authenticated 直接 PostgREST UPDATE）
- セキュリティテスト用 Node スクリプト作成（`scripts/test-pets-status-trigger.mts`）
- 第3段階 RLS テスト失敗の原因調査・テストアカウント条件の整理

## 2. pets RLS 本番化

開発用 allow-all Policy を撤去し、本番 RLS を適用した。

| Policy | 内容 |
|--------|------|
| `pets_select_breeder_own` | 本人所有 pet（全 status） |
| `pets_select_public_published` | 公開中 pet のみ |
| `pets_select_admin` | admin 全件 SELECT |
| `pets_insert_breeder_own` | 本人・draft のみ INSERT |
| `pets_update_breeder_own` | 本人 UPDATE |

admin UPDATE Policy は審査機能未実装のため未追加。

Migration: `20260807120000_harden_pets_rls.sql`

## 3. pets status Trigger

`BEFORE UPDATE OF status` トリガーで許可遷移のみ通す設計を確定し Migration を作成した。

| 主体 | 許可遷移 |
|------|---------|
| breeder（本人） | `draft → under_review` |

admin による status 変更は AD-10 / AD-11 実装時に一体設計（今回 Trigger には含めない）。

Migration: `20260807130000_enforce_pets_status_transition.sql`

## 4. status Trigger セキュリティ試験

`scripts/test-pets-status-trigger.mts` により、Server Action を経由せず authenticated JWT で直接 UPDATE し、Trigger / RLS の防御を確認した。

| 段階 | 内容 | 結果 |
|------|------|------|
| 第1段階 | 通常 UPDATE、Trigger による不正遷移拒否 | **7 passed / 0 failed** |
| 第2段階 | `draft→under_review` 許可、`under_review` からの不正遷移拒否 | **13 passed / 0 failed**（第1+2 合算） |
| 第3段階 | 他ブリーダー pet の SELECT / UPDATE 分離（RLS） | **6 passed / 0 failed** |

実行例（第3段階）:

```bash
npx tsx scripts/test-pets-status-trigger.mts --phase3
```

### 第3段階で実証したこと

- ブリーダー A はブリーダー B 所有の draft pet を SELECT できない
- ブリーダー A はブリーダー B 所有の pet を UPDATE できない
- UPDATE 試行後も対象 pet はブリーダー A から SELECT できない
- pets RLS によるブリーダー間データ分離が機能している

## 5. 第3段階 途中失敗とテストアカウント条件

第3段階の初回実行では **4 passed / 2 failed** となった。原因調査の結果、RLS 不具合ではなく **テストアカウントの前提** に問題があった。

| # | 問題 | 内容 |
|---|------|------|
| 1 | admin アカウント | `app_metadata.role = admin` により `pets_select_admin` が効き、B 所有 pet を SELECT できた |
| 2 | 同一 breeder_id | ログインアカウントが対象 pet と同じ `breeder_id` であり「他ブリーダー」テストになっていなかった |
| 3 | 解決 | **非 admin・別 breeder** のテスト専用ブリーダー A2 を作成し再試験 → **6 passed / 0 failed** |

### 今後のテストルール（第3段階）

- admin ロールを持つアカウントを使用しない
- テスト対象 pet と同じ `breeder_id` のアカウントを使用しない
- 非 admin かつ別 breeder のテスト専用アカウントを使用する
- Service Role Key を使用しない

詳細: [2026-08-07_pets_status_第3段階RLSテスト_原因調査](./2026-08-07_pets_status_第3段階RLSテスト_原因調査.md)

## 6. 関連ドキュメント

| ドキュメント | 内容 |
|-------------|------|
| [pets_status_trigger_セキュリティテスト](../10_運用手順/pets_status_trigger_セキュリティテスト.md) | 試験手順・最終結果 |
| [2026-08-07_pets_status_トリガー設計案](./2026-08-07_pets_status_トリガー設計案.md) | Trigger 設計 |
| [2026-08-07_pets_status_セキュリティレビュー](./2026-08-07_pets_status_セキュリティレビュー.md) | セキュリティレビュー |
| [2026-08-07_pets_status_第3段階RLSテスト_原因調査](./2026-08-07_pets_status_第3段階RLSテスト_原因調査.md) | 第3段階失敗原因 |

## 7. 次回予定

- 管理者犬猫掲載審査（AD-10 / AD-11）の実装
- admin UPDATE RLS / Trigger admin 遷移の一体設計
- `pet_review_logs` への申請・承認・差戻し記録の接続

## 8. 総括

pets の DB 層防御（RLS 本番化 + status Trigger）を整備し、authenticated セッションでのセキュリティ試験まで完了した。Trigger による status 遷移制御と、RLS によるブリーダー間データ分離の両方が、テスト専用データ上で期待どおり動作することを確認した。
