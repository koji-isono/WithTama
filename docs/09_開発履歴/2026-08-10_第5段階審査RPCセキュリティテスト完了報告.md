# 第5段階 審査 RPC セキュリティテスト 完了報告

日付：2026-08-10

## 1. 第5段階完了判定

**完了**

第5段階の目的（`approve_pet_for_publish` / `return_pet_review` の認可・原子性・ログ整合を authenticated JWT で確認）は、**29 passed / 0 failed** で達成されている。

### 根拠

- admin 承認・差戻し RPC が期待どおり動作（`status` / `published_at` / `pet_review_logs` 整合）
- 非 admin 拒否・空コメント拒否・二重承認拒否を確認
- Service Role 未使用（publishable key + `signInWithPassword()` のみ）
- テスト実施に伴う Migration / RLS / Trigger / RPC の追加変更なし

### 補足（第5段階の範囲外）

- **第4段階**（`test:pets-status-trigger --phase4`）は運用手順上「未実行」のまま
- **AD-10/11 UI**・Server Action 連携は未着手（第5段階の対象外）
- Migration ファイルはリポジトリ上 **未コミット（untracked）** — テスト合格とは別の Git 作業

---

## 2. テスト結果

| 項目     | 結果                           |
| -------- | ------------------------------ |
| コマンド | `npm run test:pet-review-rpcs` |
| 結果     | **29 passed / 0 failed**       |

### 準備順（判明した手順）

1. SEC-TEST 用非 admin breeder を `SEC_TEST_REVIEW_BREEDER_ID` に指定
2. `npm run prepare:sec-test-review-breeder`
3. `npm run prepare:sec-test-review-pets`
4. `.env.local` へ承認用/差戻し用 Pet ID 設定
5. `npm run test:pet-review-rpcs`

### 確認できた主な項目

- admin 認証・admin role 確認
- 承認用 Pet / 差戻し用 Pet lookup、ブリーダー公開資格（No.107）
- 非 admin から `approve_pet_for_publish` / `return_pet_review` 実行拒否（失敗後 Pet 状態・review log 不変）
- 空コメント差戻し拒否（失敗後 Pet 状態・review log 不変）
- admin `approve_pet_for_publish` 成功（`published` / `published_at` / `approved` log）
- admin `return_pet_review` 成功（`draft` / `published_at = NULL` / `returned` log・actor/comment）
- 二重承認拒否（`published` 維持・余分な `approved` log なし）

---

## 3. 更新したドキュメント

| ファイル                                                     | 更新内容                                                                            |
| ------------------------------------------------------------ | ----------------------------------------------------------------------------------- |
| `docs/09_開発履歴/2026-08.md`                                | 第5段階完了記録（29 passed、確認項目、Service Role 未使用、追加 DB 変更なし）       |
| `docs/10_運用手順/pets_status_trigger_セキュリティテスト.md` | 状態を「第5段階 実施済み」に更新、準備順5ステップ、注意事項、実施結果セクション追加 |

---

## 4. コード変更ファイル

| ファイル                                      | 状態                            |
| --------------------------------------------- | ------------------------------- |
| `scripts/test-pet-review-rpcs.mts`            | 新規（untracked）               |
| `scripts/prepare-sec-test-review-breeder.mts` | 新規（untracked）               |
| `scripts/prepare-sec-test-review-pets.mts`    | 新規（untracked）               |
| `scripts/test-pets-status-trigger.mts`        | 変更（第4段階 `--phase4` 追加） |
| `package.json`                                | 変更（npm scripts 追加）        |

---

## 5. DB 関連変更ファイル

| ファイル                                                                      | 状態              | 第5段階との関係                    |
| ----------------------------------------------------------------------------- | ----------------- | ---------------------------------- |
| `supabase/migrations/20260810110000_extend_pets_status_trigger_for_admin.sql` | 新規（untracked） | 第5段階前提                        |
| `supabase/migrations/20260810120000_create_pet_review_admin_rpcs.sql`         | 新規（untracked） | 第5段階本体                        |
| `supabase/migrations/20260810100000_add_admin_pet_photo_select_rls.sql`       | 新規（untracked） | AD-10/11 用（第5段階テスト非必須） |

---

## 6. Git コミット推奨ファイル（A）

第5段階・審査 RPC 関連を1コミットにまとめる場合:

```
scripts/test-pet-review-rpcs.mts
scripts/prepare-sec-test-review-breeder.mts
scripts/prepare-sec-test-review-pets.mts
scripts/test-pets-status-trigger.mts
package.json
docs/10_運用手順/pets_status_trigger_セキュリティテスト.md
docs/09_開発履歴/2026-08.md
supabase/migrations/20260810110000_extend_pets_status_trigger_for_admin.sql
supabase/migrations/20260810120000_create_pet_review_admin_rpcs.sql
docs/09_開発履歴/2026-08-10_管理者犬猫審査_RPC設計比較.md
docs/09_開発履歴/2026-08-10_AD-10_AD-11実装前調査.md
```

---

## 7. Git コミット除外ファイル（B）

| ファイル     | 理由                                                |
| ------------ | --------------------------------------------------- |
| `.env.local` | `.gitignore` 対象（password / Pet ID / key を含む） |

`.gitignore` 確認済み: `.env.local` は Git 管理外（`.gitignore:5`）。

---

## 8. 判断が必要なファイル（C）

| ファイル                                                                   | 判断ポイント                                                             |
| -------------------------------------------------------------------------- | ------------------------------------------------------------------------ |
| `supabase/migrations/20260810100000_add_admin_pet_photo_select_rls.sql`    | 第5段階と別フェーズ（AD-10/11 写真 RLS）。同コミットに含めるか分離するか |
| `docs/01_設計変更管理/DecisionLog.md` ほか多数の docs 変更（40+ ファイル） | 管理者審査設計・BR/AD 更新の一括変更。第5段階コミットと分離推奨          |
| `docs/09_開発履歴/2026-08-06_開発日報.md` 等                               | 別作業の履歴更新。必要に応じて別コミット                                 |

---

## 9. git status 要約

- **ブランチ:** `main`（`origin/main` と同期）
- **変更済み（M）:** 約 42 ファイル（docs 中心 + `package.json` + `test-pets-status-trigger.mts`）
- **未追跡（??）:** 第5段階スクリプト 3 本、Migration 3 本、調査レポート 2 本
- **ステージング:** なし
- **`.env.local`:** gitignore 済み・未追跡

`git add` / `commit` / `push` は未実行。

---

## 10. 残課題

- 第4段階（`npm run test:pets-status-trigger -- --phase4`）の実施
- Migration / スクリプトの Git コミット
- AD-10/11 管理者審査 UI・Server Action（RPC 呼び出し）の実装
- `submitPetForReview` 時の `pet_review_logs` `submitted` 記録の配線
- admin 犬猫写真 SELECT RLS Migration（`20260810100000`）の適用・AD-10/11 連携

---

## 11. 次の開発フェーズ候補

1. **AD-10 犬猫掲載審査一覧** — `under_review` 一覧、`submitted_at` 昇順（No.106）
2. **AD-11 犬猫掲載審査詳細** — `approve_pet_for_publish` / `return_pet_review` RPC 呼び出し
3. **第4段階 Trigger テスト** — admin 直接 UPDATE 経路の検証
4. **Git コミット整理** — 第5段階関連を1 PR、設計 doc 変更を別 PR に分割
