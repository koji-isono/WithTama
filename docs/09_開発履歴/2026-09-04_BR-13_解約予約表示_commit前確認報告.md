# BR-13 解約予約表示対応 — commit 前確認報告

| 項目   | 内容                                          |
| ------ | --------------------------------------------- |
| 確認日 | 2026-09-04                                    |
| 種別   | **commit 前最終確認**（commit / push 未実施） |
| ベース | HEAD = `a99a775`（Stripe Step 6 billing UI）  |

**機密:** Stripe Secret / Service Role Key / Customer ID 実値は本報告書に **記載しない**。

---

## 調査結果

### loaders.ts

| 項目              | 内容                                                                                                                       |
| ----------------- | -------------------------------------------------------------------------------------------------------------------------- |
| **差分内容**      | `periodEndLabel` の算出を `resolveBillingStatusPresentation()` より前に移動し、`periodEndLabel` 引数を presentation に追加 |
| **今回変更か**    | **はい** — BR-13 解約予約表示対応。解約予約時の説明文に `subscription_current_period_end` を反映するため                   |
| **Step 7 以前か** | **いいえ** — Step 7 とは無関係                                                                                             |
| **無関係か**      | **いいえ**                                                                                                                 |
| **commit対象か**  | **はい** — BR-13 commit に含めるべき                                                                                       |

#### git diff（要約）

```diff
+  const periodEndLabel = formatBillingPeriodEnd(row.subscription_current_period_end);
+
   const presentation = resolveBillingStatusPresentation({
     membershipStatus,
     subscriptionStatus: row.subscription_status,
     cancelAtPeriodEnd: row.cancel_at_period_end,
     reviewApproved,
+    periodEndLabel,
   });

-  const periodEndLabel = formatBillingPeriodEnd(row.subscription_current_period_end);
```

HEAD（Step 6）では `cancelAtPeriodEnd` は既に渡していたが、`periodEndLabel` は presentation に渡していなかった。

---

### BR-13 commit対象

**注意:** 現在の modified ファイルは **Step 7 と BR-13 が混在**している。`git add .` やファイル丸ごとの stage は避け、**hunk 単位の分離**が必要。

#### 含めるべきファイル・差分

| ファイル                                                         | 含める差分                                                                                                                                                       |
| ---------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `src/features/billing/loaders.ts`                                | **全体**                                                                                                                                                         |
| `src/features/billing/billing-display.ts`                        | **BR-13 部分のみ:** `periodEndLabel` 引数、`buildCancelScheduledDescription()`、`BILLING_ACTIVE_CANCEL_SCHEDULED_HEADLINE`、`active_cancel_scheduled` 説明文更新 |
| `src/features/billing/components/breeder-billing-view.tsx`       | **BR-13 部分のみ:** `text-amber-700` → `text-amber-800`（任意）                                                                                                  |
| `scripts/test-stripe-step6-billing-ui.mts`                       | **BR-13 部分:** #10–11, #34–42。**#14（Portal CTA）は除外**                                                                                                      |
| `docs/09_開発履歴/2026-09-04_BR-13_解約予約表示対応_実装報告.md` | **全体**（内容修正後）                                                                                                                                           |

#### HEAD（Step 6）との関係

Step 6 時点で既に以下は存在する:

- `active_cancel_scheduled` variant
- 見出し「解約予定」
- `showEndScheduledDate`（利用終了予定日ブロック）

今回 BR-13 の本質的追加は:

- **日付入り説明文**（`{日付}に利用終了予定です…`）
- **`periodEndLabel` の presentation への受け渡し**

---

### commit対象外

#### Stripe Step 7 関連（別 commit 推奨）

| ファイル                                                    | 理由                                          |
| ----------------------------------------------------------- | --------------------------------------------- |
| `src/app/api/billing/portal/`                               | Step 7 Portal API（untracked）                |
| `src/features/billing/components/billing-portal-button.tsx` | Step 7                                        |
| `src/features/billing/create-portal-session.ts`             | Step 7                                        |
| `src/features/billing/portal-constants.ts`                  | Step 7                                        |
| `src/features/billing/portal-gate.ts`                       | Step 7（`billing-display.ts` が import 依存） |
| `src/features/billing/portal-handler.ts`                    | Step 7                                        |
| `src/features/billing/portal-request.ts`                    | Step 7                                        |
| `src/features/billing/portal-urls.ts`                       | Step 7                                        |
| `scripts/test-stripe-step7-customer-portal.mts`             | Step 7 テスト（untracked）                    |
| `scripts/test-stripe-step7-portal-live.mts`                 | Step 7 live テスト                            |
| `package.json.step7-backup`                                 | Step 7 作業用バックアップ                     |

#### modified ファイル内の Step 7 差分

| ファイル                           | Step 7 差分                                                                                                        |
| ---------------------------------- | ------------------------------------------------------------------------------------------------------------------ |
| `billing-display.ts`               | `portal-gate` import、`resolvePortalPresentation()`、`showPortalCta` / `portalCtaLabel`、suspended Portal 通知削除 |
| `breeder-billing-view.tsx`         | `BillingPortalButton` import と描画ブロック                                                                        |
| `index.ts`                         | Portal 関連 export（**ファイル全体が Step 7 寄り**）                                                               |
| `test-stripe-step6-billing-ui.mts` | #14 suspended Portal CTA 期待値変更                                                                                |

#### 無関係な変更

| ファイル                                             | 理由                    |
| ---------------------------------------------------- | ----------------------- |
| `docs/00_プロジェクト全体設計/` 他多数 modified docs | BR-13 / Step 7 と無関係 |
| `tsconfig.tsbuildinfo`                               | ビルド生成物            |
| `.obsidian/`                                         | エディタ設定            |
| `supabase/.temp/`                                    | 一時ファイル            |
| その他 untracked 開発履歴（8/25〜9/02 等）           | 別作業の報告書          |

---

## ファイル分類一覧

| ファイル                                                         | 分類                    |
| ---------------------------------------------------------------- | ----------------------- |
| `src/features/billing/loaders.ts`                                | **BR-13**               |
| `src/features/billing/billing-display.ts`                        | **BR-13 + Step 7 混在** |
| `src/features/billing/components/breeder-billing-view.tsx`       | **BR-13 + Step 7 混在** |
| `src/features/billing/index.ts`                                  | **主に Step 7**         |
| `scripts/test-stripe-step6-billing-ui.mts`                       | **BR-13 + Step 7 混在** |
| `scripts/test-stripe-step7-customer-portal.mts`                  | **Step 7**              |
| Portal 関連 untracked 7 ファイル + API route                     | **Step 7**              |
| `docs/09_開発履歴/2026-09-04_BR-13_解約予約表示対応_実装報告.md` | **開発履歴（BR-13）**   |
| その他 docs / tsbuildinfo / .obsidian 等                         | **無関係**              |

---

## commit 分離方針（推奨）

### 推奨順序

1. **Commit 1: Stripe Step 7** — Portal 関連 untracked 一式 + `billing-display` / `view` / `index` / step6 #14 / step7 テスト
2. **Commit 2: BR-13 解約予約表示** — `loaders.ts` + BR-13 差分のみ + 報告書

### 現状のまま BR-13 単独 commit する場合

**不可** — `billing-display.ts` が untracked の `portal-gate.ts` に依存するため、modified のみ commit すると build 失敗。

### BR-13 単独 commit が可能になる条件

- **方針 A:** Step 7 を先 commit → 続けて BR-13
- **方針 B:** `git add -p` で BR-13 hunk のみ stage（作業ツリーに Step 7 変更は残る）

---

## テスト結果

| 項目                   | 結果                      |
| ---------------------- | ------------------------- |
| Step 6 billing UI      | **42 / 42 PASS**          |
| Step 7 Customer Portal | **38 / 38 PASS**          |
| Step 5 membership      | **33 / 33 PASS**          |
| Step 4 webhook         | **48 / 48 PASS**          |
| lint                   | **PASS**                  |
| typecheck              | **PASS**                  |
| build                  | **PASS**                  |
| Prettier（コード）     | **PASS**                  |
| Prettier（報告書 MD）  | **warn**（format 未適用） |

※ 上記は Step 7 含む **現 worktree 全体** での結果。

---

## ドキュメント修正要否

**要**

| 問題                                 | 修正内容                                                                                                     |
| ------------------------------------ | ------------------------------------------------------------------------------------------------------------ |
| `loaders.ts` 記載矛盾                | 実装報告書の「変更なし」を削除し、**変更あり**として `periodEndLabel` 渡しを追記                             |
| 変更ファイル一覧に `loaders.ts` 欠落 | サマリー・詳細両方に追加                                                                                     |
| Step 7 混在の未記載                  | 「worktree は Step 7 と BR-13 混在。BR-13 単独 commit には hunk 分離または Step 7 先行 commit が必要」と明記 |
| `index.ts` の位置づけ                | 「export 整理」→ **Step 7 Portal export が主**                                                               |
| `step7-customer-portal.mts`          | BR-13 変更ファイルではなく **Step 7 テスト** と分類                                                          |
| Prettier                             | 報告書 MD に format warn — commit 前に `prettier --write` 推奨                                               |

---

## commit 可否

| 判断       | 内容                                                                                              |
| ---------- | ------------------------------------------------------------------------------------------------- |
| **NO**     | 現状のまま BR-13 単独 commit する場合                                                             |
| **理由 1** | `billing-display.ts` / `breeder-billing-view.tsx` / `index.ts` / step6 テストに Step 7 差分が混在 |
| **理由 2** | `portal-gate.ts` 等が untracked のため、modified のみ commit すると build 不能                    |
| **理由 3** | 実装報告書に `loaders.ts` 記載矛盾あり                                                            |

| 判断       | 内容                                                         |
| ---------- | ------------------------------------------------------------ |
| **YES**    | 以下の条件を満たす場合                                       |
| **方針 A** | Step 7 を先 commit → BR-13（`loaders.ts` + 残差分 + 報告書） |
| **方針 B** | `git add -p` で BR-13 hunk のみ stage                        |

---

## 実施していないこと

- git commit / push
- revert / stash / 無関係ファイルの変更・削除
- DB Migration 追加
- Stripe / Webhook / RLS 仕様変更
- コード修正（確認のみ）

---

## 関連

- [BR-13 解約予約表示対応 実装報告](./2026-09-04_BR-13_解約予約表示対応_実装報告.md)
- [Stripe Step 7 Customer Portal 監査完了報告](./2026-09-02_Stripe-Step7_Customer-Portal_監査完了報告.md)
