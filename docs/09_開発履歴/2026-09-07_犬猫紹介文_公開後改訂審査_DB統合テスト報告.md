# 犬猫紹介文 公開後改訂審査 DB統合テスト報告

| 項目   | 内容                                                                         |
| ------ | ---------------------------------------------------------------------------- |
| 作業日 | 2026-09-07                                                                   |
| 種別   | Migration 適用後 DB 統合テスト（CASE H–X）                                   |
| 前提   | [実装報告](./2026-09-07_犬猫紹介文_公開後改訂審査_実装報告.md)               |
| 状態   | **DB 統合テスト完了**（commit / push 未実施）                                |
| 関連   | [ブラウザE2E報告](./2026-09-07_犬猫紹介文_公開後改訂審査_ブラウザE2E報告.md) |

---

## 1. 作業概要

ユーザーが Supabase Dashboard SQL Editor から Migration を手動適用済み。本セッションでは **Migration 再適用なし** で Schema / Trigger / RPC / 既存データ / CASE H–X / 回帰 / 品質ゲートを実施した。

---

## 2. Git現在地

| 項目          | 内容               |
| ------------- | ------------------ |
| branch        | `main` @ `bef2be3` |
| commit / push | **未実施**         |

---

## 3. Supabase環境確認

| 確認項目        | 結果                                               |
| --------------- | -------------------------------------------------- |
| 接続先 project  | 開発 / SEC-TEST（ref: `mahgsrtuyzgqlkoiwqky`）     |
| production 相当 | **いいえ**                                         |
| Migration 適用  | **Dashboard 手動適用済み**（本セッション未再適用） |
| Local Supabase  | 不可（Docker なし）                                |

---

## 4. Migration事前確認

対象: `supabase/migrations/20260907100000_add_pet_description_revision_review.sql`

| 項目                  | 判定 |
| --------------------- | ---- |
| 破壊的 SQL            | なし |
| default `none` / NULL | OK   |
| CHECK / trigger / RPC | OK   |
| 既存データ互換        | OK   |

---

## 5. Migration適用

| 項目                        | 結果                                              |
| --------------------------- | ------------------------------------------------- |
| Dashboard 手動適用          | **成功**（SQL Editor: Success. No rows returned） |
| CLI / `apply-sql-migration` | **未実行**（再適用禁止のため）                    |
| 本セッション再適用          | **なし**                                          |

---

## 6. Schema確認

| 対象                        | 結果                                        |
| --------------------------- | ------------------------------------------- |
| `pending_description`       | **存在**                                    |
| `description_review_status` | **存在**（default `none`）                  |
| CHECK 制約群                | **適用済み**（RPC / UPDATE プローブで整合） |

---

## 7. Trigger確認

| 対象                              | 結果     |
| --------------------------------- | -------- |
| `enforce_pets_description_update` | **動作** |

CASE S で breeder 直接 UPDATE が拒否されることを確認:

- `description` → `cannot update description on published pet outside approval RPC`
- `pending_description` → `pending description must be changed via revision RPC`
- `description_review_status` → `description review status must be changed via RPC`

---

## 8. RPC確認

| RPC                                         | 結果                      |
| ------------------------------------------- | ------------------------- |
| `save_pet_description_revision_draft`       | **存在・動作**            |
| `submit_pet_description_revision`           | **存在・動作**            |
| `approve_pet_description_revision`          | **存在・動作**            |
| `return_pet_description_revision`           | **存在・動作**            |
| `submit_pet_for_review`（description 拡張） | **動作**（20〜2000 必須） |

---

## 9. RLS確認

| 項目                                     | 結果     |
| ---------------------------------------- | -------- |
| `pets_update_breeder_own` 維持           | OK       |
| 他ブリーダー pet 改訂 RPC 拒否（CASE T） | **PASS** |
| trigger + RPC による DB 迂回防止         | OK       |
| `pet_review_logs` `description_*` INSERT | **PASS** |

---

## 10. 既存データ確認

| 項目                                           | 結果                                                                    |
| ---------------------------------------------- | ----------------------------------------------------------------------- |
| Migration 適用直後の default（`none` / NULL）  | **OK**（service role 全件: 非 default 0 件）                            |
| テスト実行後                                   | SEC-TEST pet のみ revision 状態を一時保持し得る（cleanup 処理追加済み） |
| `description` / `status` / `published_at` 破損 | **なし**                                                                |

---

## 11. CASE H〜X結果

| CASE | 内容                             | 結果     |
| ---- | -------------------------------- | -------- |
| H    | 初回公開承認フロー               | **PASS** |
| I    | published 変更案保存             | **PASS** |
| J    | none → draft                     | **PASS** |
| K    | draft → under_review + log       | **PASS** |
| L    | Public 旧 description            | **PASS** |
| M    | 管理者承認（原子的切替）         | **PASS** |
| N    | 承認後 Public 新 description     | **PASS** |
| O    | 差戻し                           | **PASS** |
| P    | 差戻し理由 log                   | **PASS** |
| Q    | returned → draft                 | **PASS** |
| R    | 再申請                           | **PASS** |
| S    | description 直接 UPDATE 拒否     | **PASS** |
| T    | 他ブリーダー pet 変更拒否        | **PASS** |
| U    | pending Public 漏洩なし          | **PASS** |
| V    | review status Public 漏洩なし    | **PASS** |
| W    | 同一文章申請拒否                 | **PASS** |
| X    | under_review 中 pending 編集不可 | **PASS** |

**実行結果:** `npm run test:pet-description-revision-db` — **63 PASS / 0 FAIL / 1 SKIP**（SKIP は同一 pet で O–R を実行する旨の情報ログのみ）

---

## 12. Trigger bypass検証

| 確認                                                | 結果     |
| --------------------------------------------------- | -------- |
| `set_config` RPC を breeder が呼べない              | **PASS** |
| `set_app_allow_published_description_update` 不存在 | **PASS** |
| `allow_published_description_update` 不存在         | **PASS** |
| CASE S 直接 UPDATE 拒否                             | **PASS** |

**結論:** 一般 breeder クライアントから session flag を設定して trigger を迂回する経路は **確認されず**。

---

## 13. SECURITY DEFINER検証

| 項目                    | 結果                             |
| ----------------------- | -------------------------------- |
| `SET search_path = ''`  | Migration 定義どおり             |
| `auth.uid()` 必須       | OK                               |
| ownership 検証          | OK（CASE T）                     |
| admin 判定（admin RPC） | OK（breeder → `admin required`） |
| anon EXECUTE 拒否       | OK（未認証 RPC 不可）            |
| SQL injection 余地      | パラメータ bound（設計どおり）   |

---

## 14. pet_review_logs

| action                                     | 結果                     |
| ------------------------------------------ | ------------------------ |
| `description_submitted`                    | **PASS**                 |
| `description_approved`                     | **PASS**                 |
| `description_returned`                     | **PASS**（comment 必須） |
| 既存 `submitted` / `approved` / `returned` | **PASS**（読取可）       |

---

## 15. Public回帰

| テスト                           | 結果                                                   |
| -------------------------------- | ------------------------------------------------------ |
| `npm run test:public-pet-detail` | **14 PASS / 0 FAIL**（3 未検証は optional env 未設定） |
| `npm run test:public-pet-read`   | **22 PASS / 0 FAIL**（5 未検証は optional env 未設定） |

`pending_description` / `description_review_status` は Public View に **非含有**（CASE U/V でも確認）。

---

## 16. 初回掲載審査回帰

| テスト                               | 結果                 |
| ------------------------------------ | -------------------- |
| `npm run test:submit-pet-for-review` | **25 PASS / 0 FAIL** |
| `npm run test:pet-review-rpcs`       | **29 PASS / 0 FAIL** |

`submit_pet_for_review` の description 20〜2000 必須拡張は意図どおり。prepare 脚本に draft description 設定を追加して回帰 PASS。

---

## 17. 自動テスト

| コマンド                                        | 結果                          |
| ----------------------------------------------- | ----------------------------- |
| `npm run test:pet-description-validation`       | **9/9 PASS**                  |
| `npm run prepare:sec-test-description-revision` | **PASS**                      |
| `npm run test:pet-description-revision-db`      | **63/63 PASS**（1 SKIP 情報） |

---

## 18. lint

**PASS**（`npm run lint`）

---

## 19. typecheck

**PASS**（`npm run typecheck`）

---

## 20. format

| 対象                             | 結果                           |
| -------------------------------- | ------------------------------ |
| 本セッション変更脚本（Prettier） | **PASS**                       |
| リポジトリ全体 `format:check`    | 未実行（無関係 docs 整形なし） |

---

## 21. build

**PASS**（`npm run build`）

---

## 22. 修正ファイル

本セッション（DB 統合テスト再開）:

| ファイル                                            | 内容                                                                      |
| --------------------------------------------------- | ------------------------------------------------------------------------- |
| `scripts/prepare-sec-test-description-revision.mts` | under_review から published + description 準備、revision 状態リセット改善 |
| `scripts/prepare-sec-test-submit-pet.mts`           | draft description 20 文字設定（`submit_pet_for_review` 拡張対応）         |
| `scripts/test-pet-description-revision-db.mts`      | CASE T 判定修正、pet 自動解決、cleanup 追加                               |
| `package.json`                                      | `prepare:sec-test-submit-pet` を revision DB テスト前に実行               |
| 本報告 MD                                           | 実測結果で更新                                                            |

---

## 23. Migration適用環境

| 項目            | 内容                                  |
| --------------- | ------------------------------------- |
| 適用方法        | Supabase Dashboard SQL Editor（手動） |
| 適用先          | 開発 Supabase `mahgsrtuyzgqlkoiwqky`  |
| production 相当 | **いいえ**                            |

---

## 24. 未確認事項

- optional `SEC_TEST_PUBLIC_*` 未設定による Public テスト「未検証」項目（環境要因）
- `save_pet_description_revision_draft` で `returned` + `pending=NULL` を直接クリアする操作（CHECK 上不可 — 設計どおり。prepare / cleanup は resubmit → approve でリセット）

---

## 25. STOP条件

| 条件                               | 該当       |
| ---------------------------------- | ---------- |
| production 相当のみ                | **非該当** |
| Migration 破壊的変更               | **非該当** |
| pending Public 漏洩                | **非該当** |
| trigger bypass 可能                | **非該当** |
| breeder description 直接 UPDATE 可 | **非該当** |
| 大規模設計変更必要                 | **非該当** |

---

## 26. Git status

- branch: `main`
- commit: **未実施**
- push: **未実施**

---

## 27. 結論

1. Dashboard 手動 Migration 適用 **成功**。
2. Schema / Trigger / RPC / SECURITY DEFINER / RLS は設計どおり動作。
3. **CASE H–X 全 PASS**（最重要 S / U / V / trigger bypass 含む）。
4. 初回掲載審査・Public 回帰 **PASS**。
5. lint / typecheck / build **PASS**。
6. **commit / push 未実施**（指示どおり）。

---

## 28. ブラウザE2E（追記）

正常系ブラウザ E2E は別報告に記録。差戻しフローは **DB PASS / 手動未実施**。

→ [ブラウザE2E報告](./2026-09-07_犬猫紹介文_公開後改訂審査_ブラウザE2E報告.md)
