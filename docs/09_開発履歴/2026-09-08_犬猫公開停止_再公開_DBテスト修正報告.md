# WithTama 犬猫公開停止・再公開 DBテスト修正報告

## 1. 修正目的

Migration および機能実装は正常だったが、`test:pet-listing-pause-db` 実行時にテスト脚本側で 3 点の問題が確認された。本対応では **テスト脚本のみ** を修正し、CASE A〜S を FAIL 0 で再実行する。

## 2. 修正ファイル

| ファイル                                         | 変更内容                                                    |
| ------------------------------------------------ | ----------------------------------------------------------- |
| `scripts/prepare-sec-test-pet-listing-pause.mts` | description 直接 UPDATE 削除、draft / under_review 準備追加 |
| `scripts/test-pet-listing-pause-db.mts`          | CASE H / L / C / D 修正、pet 解決ロジック改善               |

**変更していないもの:** `supabase/migrations/**`, `src/**`, DB, UI, docs（本報告 MD を除く）

## 3. prepare修正

| 修正前                                         | 修正後                                                                                           |
| ---------------------------------------------- | ------------------------------------------------------------------------------------------------ |
| `published` pet の `description` を直接 UPDATE | 既存 `description` の長さを **読み取りのみ** で検証（`length >= 20`）                            |
| draft / under_review pet 未準備                | 既存 `[SEC-TEST]` draft を探索、under_review は再利用または `submit_pet_for_review` で安全に準備 |

出力例:

```
SEC_TEST_LISTING_PAUSE_PET_ID=7d9e363d-2468-4c89-a585-3a5eb3a85982
SEC_TEST_LISTING_PAUSE_DRAFT_PET_ID=b60814c0-84dd-4fe7-8662-c22f09e9e7ef
SEC_TEST_LISTING_PAUSE_UNDER_REVIEW_PET_ID=85ef22b3-31e4-4739-96a0-0146d40961c3
```

## 4. CASE H修正

| 修正前                                                             | 修正後                                                                                               |
| ------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------- |
| buyer resume 時点で pet が `published` → `invalid pet status` のみ | breeder が pet を `paused` にした後、buyer が `resume_pet_listing` → **`unauthorized`** で拒否を検証 |
|                                                                    | テスト末尾で breeder が resume して状態を復元                                                        |

追加チェック:

- `CASE H setup: breeder pauses pet for resume auth test`
- `CASE H setup: pet is paused before buyer resume`
- `CASE H cleanup: breeder resumes pet`

## 5. CASE L修正

| 修正前                              | 修正後                                                                         |
| ----------------------------------- | ------------------------------------------------------------------------------ |
| `queueCount is not defined`（typo） | `countDescriptionRevisionQueue()` ヘルパー追加、`adminQueueCount` を正しく参照 |
| キュー確認 1 件のみ                 | pause 前 `count=1`、pause 後 `count=0`、resume 後も `count=0` を正式 PASS      |

## 6. CASE C/D

prepare / test 双方で draft / under_review pet を自動解決。

| CASE                          | 結果     | pet                                                                     |
| ----------------------------- | -------- | ----------------------------------------------------------------------- |
| C `draft → pause` 拒否        | **PASS** | `[SEC-TEST] Submit RPC With Photo Pet #10`                              |
| D `under_review → pause` 拒否 | **PASS** | `[SEC-TEST] Submit RPC With Photo Pet #9`（既存 under_review を再利用） |

## 7. CASE F/G

| CASE                | 結果     | 理由                                               |
| ------------------- | -------- | -------------------------------------------------- |
| F 他 breeder pause  | **SKIP** | `SEC_TEST_OTHER_BREEDER_EMAIL` / `PASSWORD` 未設定 |
| G 他 breeder resume | **SKIP** | 同上                                               |

環境に他 breeder 資格情報が存在する場合のみ実行する設計（変更なし）。

## 8. CASE A〜S結果

| CASE | 内容                                           | 結果     |
| ---- | ---------------------------------------------- | -------- |
| A    | `published → paused`                           | **PASS** |
| B    | `paused → published`                           | **PASS** |
| C    | `draft → pause` 拒否                           | **PASS** |
| D    | `under_review → pause` 拒否                    | **PASS** |
| E    | `published → resume` 拒否                      | **PASS** |
| F    | 他 breeder pause 拒否                          | **SKIP** |
| G    | 他 breeder resume 拒否                         | **SKIP** |
| H    | buyer pause / resume 拒否                      | **PASS** |
| I    | anon pause / resume 拒否                       | **PASS** |
| J    | pause 二重実行拒否                             | **PASS** |
| K    | resume 二重実行拒否                            | **PASS** |
| L    | 紹介文審査中 pause で pending 破棄・キュー除外 | **PASS** |
| M    | description 保持                               | **PASS** |
| N    | published_at 保持                              | **PASS** |
| O    | inquiry 保持                                   | **PASS** |
| P    | visit 保持                                     | **PASS** |
| Q    | favorite 保持                                  | **PASS** |
| R    | paused Public View 非表示                      | **PASS** |
| S    | resume Public View 再表示                      | **PASS** |

**集計:** `44 passed / 0 failed / 2 skipped`

## 9. Security結果

| 観点                                   | 結果                 |
| -------------------------------------- | -------------------- |
| RPC 存在                               | PASS                 |
| breeder 本人 pause / resume            | PASS                 |
| 不正 status 拒否（C/D/E/J/K）          | PASS                 |
| buyer 認可拒否（paused pet 対 resume） | PASS                 |
| anon permission denied                 | PASS                 |
| 他 breeder                             | SKIP（資格情報なし） |

## 10. 回帰テスト

| コマンド                                   | 結果                            |
| ------------------------------------------ | ------------------------------- |
| `npm run test:pets-status-trigger`         | **PASS**（16/16）               |
| `npm run test:pet-description-revision-db` | **PASS**（56/56、2 skipped）    |
| `npm run test:public-pet-read`             | **PASS**（22/22、5 unverified） |
| `npm run test:breeder-pets-list-ui`        | **PASS**（22/22）               |

## 11. lint/typecheck/build

| コマンド                              | 結果     |
| ------------------------------------- | -------- |
| `npm run lint`                        | **PASS** |
| `npm run typecheck`                   | **PASS** |
| `npm run build`                       | **PASS** |
| Prettier check（修正 2 ファイルのみ） | **PASS** |

## 12. 残SKIP

| SKIP   | 理由                                               |
| ------ | -------------------------------------------------- |
| CASE F | `SEC_TEST_OTHER_BREEDER_EMAIL` / `PASSWORD` 未設定 |
| CASE G | 同上                                               |

環境資格情報不足による SKIP のみ。FAIL は 0。

## 13. 変更していないもの

- `supabase/migrations/**`
- `src/**`（UI / Repository / Service 等）
- DB / RLS / RPC（本番定義）
- DecisionLog / BR-10 / pets.md
- 無関係 docs / working tree 上の他変更

## 14. Git状態

| 項目         | 状態                                                                                                 |
| ------------ | ---------------------------------------------------------------------------------------------------- |
| 変更（本件） | `scripts/prepare-sec-test-pet-listing-pause.mts`, `scripts/test-pet-listing-pause-db.mts`, 本報告 MD |
| commit       | **未実施**                                                                                           |
| push         | **未実施**                                                                                           |

## 15. 結論

テスト脚本 3 点の修正により `npm run test:pet-listing-pause-db` は **FAIL 0**（44 passed / 2 skipped）となった。SKIP は他 breeder 資格情報未設定のみ。回帰テスト・lint・typecheck・build も PASS。機能コード・Migration・DB・UI は変更していない。ブラウザ E2E は別途実施可能。
