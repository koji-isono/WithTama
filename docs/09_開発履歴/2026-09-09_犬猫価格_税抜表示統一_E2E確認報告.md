# WithTama 犬猫価格 税抜表示統一 E2E確認報告

| 項目      | 内容                             |
| --------- | -------------------------------- |
| 確認日    | 2026-09-09                       |
| 基準 HEAD | `cb6a657`（作業時点・未 commit） |
| 種別      | commit 前 E2E / ブラウザ表示確認 |
| commit    | **未実施**（指示により）         |
| push      | **未実施**（指示により）         |

---

## 1. 目的

Decision No.153 に基づく犬猫価格の税抜表示統一について、実画面および自動テストで以下を確認する。

- 公開・ブリーダー・管理者各画面で `xxx,xxx円（税抜）` 形式が表示されること
- ブリーダー入力画面（BR-08 / BR-11）から「税込」誤表記が消え、税抜入力であることが明確なこと
- DB / Migration / Stripe / 税計算に変更がないこと

---

## 2. 実行環境

| 項目         | 内容                                                 |
| ------------ | ---------------------------------------------------- |
| OS           | Windows 10 (26200)                                   |
| Node         | プロジェクト既存環境                                 |
| Dev server   | `npm run dev` → `http://localhost:3000`（Turbopack） |
| 環境変数     | `.env.local`（SEC_TEST_* 認証情報含む）              |
| DB           | リモート Supabase（既存 SEC-TEST データ使用）        |
| ブラウザ確認 | Node `fetch` + Supabase SSR Cookie 認証（HTML 取得） |

---

## 3. DB依存テスト

| Script                      | 結果           | 備考                                                                          |
| --------------------------- | -------------- | ----------------------------------------------------------------------------- |
| `test:pet-price-display`    | **13/13 PASS** | formatter / 静的配線 / Stripe 非連動                                          |
| `test:public-pets-page`     | **6/6 PASS**   | `prepare:sec-test-public-read` 含む                                           |
| `test:public-pet-detail`    | **13/13 PASS** | 4 unverified（draft 等の env 未設定ケース）                                   |
| `test:buyer-favorites-page` | **11/12 PASS** | **1 FAIL** — `12. inquiries/visits still coming soon`（**今回変更と無関係**） |
| `test:breeder-pets-list-ui` | **22/22 PASS** | 公開停止 UI 含む（価格表示は静的 `formatPetPrice` 配線確認済み）              |

**admin pet review 専用ページテスト:** `package.json` に AD-11 HTML 確認用 script は存在しない。`test:pet-review-rpcs` は RPC セキュリティテストであり DB 状態を消費するため、本確認では **未実行**（HTML E2E で代替）。

---

## 4. PU-01

**URL:** `/pets`

| 確認項目                     | 結果                |
| ---------------------------- | ------------------- |
| HTTP 200                     | PASS                |
| `100,000円（税抜）` 等の表示 | PASS（22 件マッチ） |
| 「税込」文字列なし           | PASS                |

---

## 5. PU-02

**URL:** `/pets/abb5fb1c-21ce-4e73-9006-bb74bf30b393`

| 確認項目                 | 結果 |
| ------------------------ | ---- |
| HTTP 200                 | PASS |
| 価格 `100,000円（税抜）` | PASS |
| 「税込」文字列なし       | PASS |

---

## 6. BY-03

**URL:** `/buyer/favorites`（SEC_TEST_BUYER ログイン）

| 確認項目                                    | 結果                                                                  |
| ------------------------------------------- | --------------------------------------------------------------------- |
| ログイン成功（200、login リダイレクトなし） | PASS                                                                  |
| 一覧上の価格 `…円（税抜）`                  | **未確認** — お気に入り **0 件**（空状態表示）                        |
| 静的検証（`formatPetPrice` 利用）           | PASS（`test:buyer-favorites-page` + `test:pet-price-display` CASE G） |

**DB 変更:** なし（お気に入り追加は実施せず）。

---

## 7. BR-08

**URL:** `/breeder/pets/new`（SEC_TEST_BREEDER ログイン）

| 確認項目                     | 結果                |
| ---------------------------- | ------------------- |
| HTTP 200                     | PASS                |
| ラベル「販売価格（税抜）」   | PASS                |
| 「税込」誤表記なし           | PASS                |
| description「円単位の整数…」 | PASS（HTML 内確認） |

---

## 8. BR-11

**URL:** `/breeder/pets/7d9e363d-2468-4c89-a585-3a5eb3a85982/edit`（SEC_TEST_BREEDER 所有 pet）

| 確認項目                   | 結果 |
| -------------------------- | ---- |
| HTTP 200                   | PASS |
| ラベル「販売価格（税抜）」 | PASS |
| 「税込」誤表記なし         | PASS |

---

## 9. BR-10

**URL:** `/breeder/pets`（SEC_TEST_BREEDER ログイン）

| 確認項目                          | 結果              |
| --------------------------------- | ----------------- |
| HTTP 200                          | PASS              |
| `…円（税抜）` / `価格未設定` 表示 | PASS（18 ラベル） |
| 「税込」文字列なし                | PASS              |

---

## 10. AD-11

**URL:** `/admin/pets/reviews/079d446b-4303-460b-957b-7830cc08e69e`（`SEC_TEST_ADMIN_RETURN_PET_ID`、status=`under_review`）

| 確認項目                 | 結果                                 |
| ------------------------ | ------------------------------------ |
| HTTP 200                 | PASS                                 |
| 価格 `100,000円（税抜）` | PASS                                 |
| NULL 価格は `—`          | PASS（同一 HTML 内）                 |
| 税込参考価格なし         | PASS（「税込参考」「税込価格」なし） |
| 「税込」文字列なし       | PASS                                 |

**補足:** `SEC_TEST_ADMIN_APPROVE_PET_ID`（`abb5fb1c-…`）は **published** のため審査詳細 URL は **404**。審査中 pet ID で再確認済み。

---

## 11. スマートフォン表示

| 画面  | UA               | 結果                                                       |
| ----- | ---------------- | ---------------------------------------------------------- |
| PU-01 | iPhone Safari UA | PASS — `…円（税抜）` 表示、レイアウト崩れなし（HTML 確認） |
| PU-02 | iPhone Safari UA | PASS — 価格表示正常                                        |

---

## 12. price_comment

**URL:** `/pets/bb3c9725-5615-44aa-8913-286348203a41`（`price_comment` あり published pet）

| 確認項目                           | 結果                                         |
| ---------------------------------- | -------------------------------------------- |
| 価格 `…円（税抜）` と comment 共存 | PASS                                         |
| レイアウト崩れ                     | なし（HTML 上、価格・comment 両方 DOM 存在） |

---

## 13. 回帰

| 領域           | 結果 | 備考                       |
| -------------- | ---- | -------------------------- |
| 犬猫カード     | PASS | PU-01 複数カード           |
| 詳細画面       | PASS | PU-02 + price_comment pet  |
| お気に入り     | 部分 | 空状態のみ（価格行未表示） |
| ブリーダー一覧 | PASS | BR-10 18 件                |
| 管理者詳細     | PASS | AD-11 under_review pet     |
| スマホ幅       | PASS | PU-01 / PU-02              |

`src/` 全体 grep：**「税込」文字列 0 件**（ペット価格関連の誤表記残存なし）。

---

## 14. 修正有無

| 項目           | 内容                                                                              |
| -------------- | --------------------------------------------------------------------------------- |
| 追加修正       | **なし**                                                                          |
| 一時スクリプト | `scripts/_e2e-pet-price-browser.mts` を確認後 **削除済み**（package.json 未登録） |

---

## 15. 品質確認

修正なしのため **再実行なし**。実装時点の結果を参照:

| チェック                 | 実装時結果                      |
| ------------------------ | ------------------------------- |
| `test:pet-price-display` | 13/13 PASS（本確認でも再 PASS） |
| `npm run lint`           | PASS（実装報告時）              |
| `npm run typecheck`      | PASS（実装報告時）              |
| `npm run build`          | PASS（実装報告時）              |
| Prettier（変更ファイル） | PASS（実装報告時）              |

---

## 16. DB

| 項目                        | 結果                                        |
| --------------------------- | ------------------------------------------- |
| `pets.price` = 税抜 integer | **維持**（`test:pet-price-display` CASE K） |
| 本確認での DB 書き込み      | **なし**                                    |
| 新規犬猫作成                | **なし**                                    |

---

## 17. Stripe

| 項目                     | 結果                        |
| ------------------------ | --------------------------- |
| Stripe コード変更        | **なし**                    |
| 犬猫価格と Checkout 連動 | **なし**（CASE L 確認済み） |

---

## 18. Migration

| 項目                | 結果     |
| ------------------- | -------- |
| 新規 Migration      | **なし** |
| 既存 Migration 変更 | **なし** |

---

## 19. 未確認事項

1. **BY-03 一覧上の価格表示** — SEC_TEST_BUYER のお気に入り 0 件のため、カード上の `…円（税抜）` を実 HTML で未確認（コンポーネント静的検証は PASS）
2. **`test:buyer-favorites-page` 12 番** — inquiries/visits coming soon チェック FAIL（既知・今回スコープ外）
3. **`test:pet-review-rpcs`** — DB 状態消費のため未実行（AD-11 HTML で代替確認）
4. **法務・税務専門家確認** — Decision No.153 記載どおり未実施（PO 判断 Q2=A）

---

## 20. 結論

### 判定: **CONDITIONAL PASS**

**理由:**

- PU-01 / PU-02 / BR-08 / BR-11 / BR-10 / AD-11 / スマホ / price_comment は **実 HTML で税抜表示を確認**
- ブリーダー入力画面から「税込」誤表記 **なし**
- 重大レイアウト崩れ **なし**
- DB / Migration / Stripe **変更なし**
- 価格関連自動テスト **PASS**

**CONDITIONAL とした理由:**

- BY-03 はログイン成功・空状態確認のみ。お気に入り 0 件のため **一覧カード上の価格表示は未確認**（静的配線は PASS）

**commit / push:** 未実施（指示どおり）
