# WithTama 犬猫価格 税抜表示統一 実装報告

| 項目      | 内容                                  |
| --------- | ------------------------------------- |
| 実装日    | 2026-09-09                            |
| 基準 HEAD | `cb6a657`（作業時点）                 |
| 種別      | 表示ラベル統一 + Decision No.153 追加 |
| commit    | **未実施**（指示により）              |

---

## 1. 実装概要

Product Owner 判断に基づき、犬猫販売価格（`pets.price`）を **全ロール税抜表示** に統一。WithTama 側での税込計算・税率保持・Stripe 連携は行わない。

**表示標準形式（Q1=A）:** `250,000円（税抜）` / NULL → `価格未設定` / 0 → `0円（税抜）`

---

## 2. Product Decision

| 項目 | 判断                                                  |
| ---- | ----------------------------------------------------- |
| Q1   | **A** — 数値直付け `250,000円（税抜）`                |
| Q2   | **A** — Decision No.153 を専門家確認前に登録、UI 先行 |
| Q3   | **C** — 専門家 NG 時は **再 Decision**                |

---

## 3. Decision No.153

| 項目       | 内容                                                                                                                  |
| ---------- | --------------------------------------------------------------------------------------------------------------------- |
| 番号       | **153**（DecisionLog 最新 No.152 の次連番を確認済み）                                                                 |
| 状態       | **DecisionLog に追加済み**（既存 No.140/141/142/146 本文は未改訂）                                                    |
| 要点       | 全ロール税抜表示、税計算なし、犬猫価格 Stripe 不使用、Migration 不要                                                  |
| 専門家確認 | 購入希望者向け税抜のみ掲載の適法性 — **税理士・弁護士または専門家への確認が必要**。確認結果により仕様変更の可能性あり |
| NG 時      | 再 Decision（本実装を無理に維持しない）                                                                               |

---

## 4. 変更ファイル

### 実装（src）

| ファイル                                                     | 変更                                             |
| ------------------------------------------------------------ | ------------------------------------------------ |
| `src/features/pets/list-format.ts`                           | `formatPetPrice()` → `…円（税抜）`               |
| `src/features/admin/format.ts`                               | `formatAdminPetPrice()` → 同上形式               |
| `src/features/pets/components/pet-draft-form-fields.tsx`     | ラベル「販売価格（税抜）」、description 税込削除 |
| `src/features/pets/components/breeder-pets-list-content.tsx` | （formatter 経由で税抜表示）                     |
| `src/features/admin/components/admin-pet-review-detail.tsx`  | （loader → formatter 経由）                      |

**変更なし:** `validation.ts`, `repository.ts`, `service.ts`, `public-repository.ts`, `src/features/billing/**`

### テスト

| ファイル                             | 変更                                     |
| ------------------------------------ | ---------------------------------------- |
| `scripts/test-pet-price-display.mts` | **新規** — CASE A〜L                     |
| `package.json`                       | `test:pet-price-display` script 1 行追加 |

### 設計

| ファイル                              | 変更                 |
| ------------------------------------- | -------------------- |
| `docs/01_設計変更管理/DecisionLog.md` | Decision No.153 追加 |
| `docs/04_画面設計/PU-01_*.md`         | 税抜表示に更新       |
| `docs/04_画面設計/PU-02_*.md`         | 同上                 |
| `docs/04_画面設計/BY-03_*.md`         | 同上                 |
| `docs/04_画面設計/BR-10_*.md`         | 同上                 |
| `docs/04_画面設計/BR-11_*.md`         | 同上                 |
| `docs/04_画面設計/AD-11_*.md`         | 同上                 |
| `docs/05_データベース設計/pets.md`    | No.153 参照追加      |

---

## 5. formatter

**採用: 案 A** — `formatPetPrice()` 自体を税抜表示へ変更。

**理由:** 全ロール（Public / Buyer / Breeder / Admin）が税抜表示のため、別 formatter 乱立不要。PU-01/02/BY-03/BR-10 は import 変更なしで自動反映。

`formatAdminPetPrice()` も同形式（NULL は admin 従来どおり `—`）。

---

## 6. PU-01

- `public-pet-card.tsx` → `formatPetPrice(pet.price)`
- **変更後:** `250,000円（税抜）` / NULL → `価格未設定`

---

## 7. PU-02

- `public-pet-detail-view.tsx` → `formatPetPrice(detail.price)`
- `price_comment` — **変更なし**（別行表示）

---

## 8. BY-03

- `buyer-favorite-pet-card.tsx` → PU-01 と同系

---

## 9. BR-08 / BR-11

- 共通 `pet-draft-form-fields.tsx`
- label: **販売価格（税抜）**
- description: **円単位の整数。未入力でも登録できます。**
- 「税込」表記 — **削除**

---

## 10. BR-10

- `breeder-pets-list-content.tsx` → `formatPetPrice(pet.price)`
- 表示: `250,000円（税抜）`

---

## 11. AD-11

- `admin/loaders.ts` → `formatAdminPetPrice(petRow.price)` → `priceLabel`
- 表示: `250,000円（税抜）`（税込参考 — **追加なし**）
- AD-10 — **変更なし**

---

## 12. DB

| 項目                | 結果                         |
| ------------------- | ---------------------------- |
| `pets.price` schema | **変更なし**                 |
| 保存値              | **変更なし**（税抜 integer） |
| validation          | **変更なし**                 |

---

## 13. Stripe

| 項目                        | 結果         |
| --------------------------- | ------------ |
| `src/features/billing/**`   | **変更なし** |
| Checkout / Webhook / Portal | **変更なし** |
| 新規 env                    | **なし**     |

---

## 14. price_comment

- 構造・validation・placeholder — **変更なし**
- PU-02 で価格行（税抜）の下に別行表示 — レイアウト問題なし

---

## 15. 法務・税務注記

本実装は Product Owner 判断（Decision No.153）に基づく。

**購入希望者向けに税抜価格のみを掲載することの適法性**について、**税理士・弁護士または専門家への確認が必要**。確認結果により仕様変更（再 Decision）する可能性がある。

---

## 16. テスト

`npm run test:pet-price-display`

| CASE | 内容                          | 結果 |
| ---- | ----------------------------- | ---- |
| A    | null → 価格未設定             | PASS |
| B    | 0 → 0円（税抜）               | PASS |
| C    | 250000 → 250,000円（税抜）    | PASS |
| D    | 9999999 → 9,999,999円（税抜） | PASS |
| E    | PU-01 formatPetPrice          | PASS |
| F    | PU-02                         | PASS |
| G    | BY-03                         | PASS |
| H    | BR-08/11 税込表記なし         | PASS |
| I    | BR-10                         | PASS |
| J/Jb | AD-11 / formatAdminPetPrice   | PASS |
| K    | DB 保存値不変                 | PASS |
| L    | Stripe 変更なし               | PASS |

**13 / 13 PASS**

---

## 17. 回帰

| テスト                            | 結果         |
| --------------------------------- | ------------ |
| `test:pet-description-validation` | 9 / 9 PASS   |
| `test:breeder-pets-list-ui`       | 22 / 22 PASS |

※ DB 依存 public / admin テストは本報告時未実行（env 要）。formatter 変更のみのため影響限定的。

---

## 18. lint

`npm run lint` — **PASS**

---

## 19. typecheck

`npm run typecheck` — **PASS**

---

## 20. Prettier

変更ファイル 16 件に `prettier --check` — **PASS**（repo 全体は未整形ファイルにより FAIL の可能性あり — CI は commit 対象のみ評価）

---

## 21. build

`npm run build` — **PASS**

---

## 22. Migration

**なし**（作成・適用なし）

---

## 23. 未対応

| 項目                               | 理由                         |
| ---------------------------------- | ---------------------------- |
| 専門家確認                         | product / 法務タスク（並行） |
| 専門家 NG 時の税込表示             | 再 Decision 待ち             |
| `price_comment` placeholder 見直し | 今回スコープ外               |
| commit / push                      | ユーザー指示により未実施     |
| public-pet-read 等 DB E2E          | 本報告時未実行               |

---

## 24. Git状態

- 本機能関連ファイル: modified / 新規（実装報告 MD 含む）
- 無関係 working tree 変更: **保持**
- commit / push: **未実施**

---

## 25. 結論

Decision No.153 に基づき、犬猫価格の **税抜表示統一** を最小 diff で実装。DB / Migration / Stripe / 税計算は **変更なし**。テスト・lint・typecheck・build **PASS**。

専門家確認結果次第で **再 Decision** の可能性あり。commit / push は次工程。
