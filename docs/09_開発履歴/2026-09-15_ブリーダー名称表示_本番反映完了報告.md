# ブリーダー名称表示 本番反映完了報告

| 項目     | 内容                                                                 |
| -------- | -------------------------------------------------------------------- |
| 作業日   | 2026-09-15                                                           |
| 種別     | **Git 反映 + Vercel Production デプロイ完了**                        |
| 前提     | [調査・修正報告](./2026-09-15_ブリーダー名称表示_調査修正報告.md)    |

**機密:** token / password / API key は記載しない。

---

## 1. commit hash

| 種別           | hash       | message                                              |
| -------------- | ---------- | ---------------------------------------------------- |
| **機能修正**   | **`8a96a53`** | `fix(breeder): refresh header display name after profile save` |
| 完了報告 MD    | （本 commit 後に追記）                               |

### 機能修正 commit 対象ファイル

| ファイル | 変更 |
| -------- | ---- |
| `src/features/breeder-profile/service.ts` | `revalidatePath("/breeder", "layout")` |
| `src/features/breeder-profile/components/basic-info-step-form.tsx` | `router.refresh()` |
| `scripts/test-breeder-header-display-name.mts` | S13 / S14 |
| `docs/09_開発履歴/2026-09-15_ブリーダー名称表示_調査修正報告.md` | 調査報告 |

---

## 2. push 結果

| 項目   | 内容 |
| ------ | ---- |
| branch | `main` |
| remote | `origin` |
| range  | `a6b6406..8a96a53` |
| 結果   | **成功** |

---

## 3. GitHub Actions 結果

### 3.1 機能修正 commit `8a96a53` — Run **#88**

| 項目       | 内容 |
| ---------- | ---- |
| run id     | `34917434409` |
| URL        | https://github.com/koji-isono/WithTama/actions/runs/34917434409 |
| head SHA   | `8a96a5337116dbea8908d1208e18b1e8cc70f5ab` |
| 結果       | **SUCCESS** |

| ステップ     | 結果 |
| ------------ | ---- |
| npm ci       | success |
| lint         | success |
| typecheck    | success |
| format:check | success |
| build        | success |

### 3.2 完了報告 MD commit — Run **#89**（push 後追記）

（本 MD commit / push 後に CI 結果を追記）

---

## 4. Vercel Production デプロイ結果

| 項目 | 内容 |
| ---- | ---- |
| 対象 commit | `8a96a5337116dbea8908d1208e18b1e8cc70f5ab` |
| GitHub Status | **success** — `Deployment has completed` |
| Vercel URL | https://vercel.com/koji-isono/with-tama/5nims3XTKuTfMJUvUoG1BGdEQZjD |
| Production 反映 | **完了**（main push 連動デプロイ） |

---

## 5. 実施したテスト（commit 前）

| コマンド | 結果 |
| -------- | ---- |
| `npm run lint` | **PASS** |
| `npm run typecheck` | **PASS** |
| `npm run test:breeder-header-display-name` | **27/27 PASS** |
| `npm run test:breeder-profile-initial-values` | **33/33 PASS** |
| `npm run build` | **PASS** |

---

## 6. DB 変更の有無

| 項目 | 変更 |
| ---- | ---- |
| Production Supabase DB | **なし** |
| データ INSERT / UPDATE / DELETE | **なし**（アプリ経由の通常利用を除く） |

---

## 7. Migration 変更の有無

| 項目 | 変更 |
| ---- | ---- |
| 新規 Migration | **なし** |
| 既存 Migration 修正 | **なし** |
| `db push` | **未実施** |

---

## 8. RLS / GRANT 変更の有無

| 項目 | 変更 |
| ---- | ---- |
| RLS ポリシー | **なし** |
| table GRANT | **なし** |

（2026-09-14 適用済み `20260914100000_grant_phase1_table_privileges.sql` を利用）

---

## 9. Production で確認すべき操作

Vercel Production デプロイ完了後、PO / 検証担当で以下を実施:

1. ブリーダー login
2. BR-09 Step 1「基本情報」で **屋号・事業所名** を入力
3. 「保存して次へ」クリック
4. **F5 なし**で右上表示が入力した屋号に変わることを確認
5. Step 2 以降・ダッシュボード遷移後も屋号が維持されることを確認

---

## 10. 残課題

| 項目 | 状態 |
| ---- | ---- |
| Production 目視 E2E（Step 1 保存 → ヘッダー即時反映） | **PO 確認待ち** |
| 他 Step フォームへの `router.refresh` 統一 | **任意**（ヘッダー表示名に影響するのは Step 1 のみ） |
| 本報告 MD commit 後 CI #89 結果 | **push 後追記** |

---

## 11. main / origin/main

| 項目 | 値 |
| ---- | -- |
| 機能修正 push 後 HEAD | `8a96a5337116dbea8908d1208e18b1e8cc70f5ab` |
