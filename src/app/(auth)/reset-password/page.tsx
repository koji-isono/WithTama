import { ResetPasswordPageContent } from "./reset-password-form";

export const metadata = {
  title: "パスワード再設定",
};

export default function ResetPasswordPage() {
  return <ResetPasswordPageContent />;
}
